import { FormatecurrencyPipe } from './formatecurrency.pipe';

describe('FormatecurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatecurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
